<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_map_black_24dp"
            android:title="@string/menu_home" />
        <item
            android:id="@+id/nav_setRoute"
            android:icon="@drawable/ic_bus"
            android:title="Set Route Number" />
        <item
            android:id="@+id/nav_pas_profile"
            android:icon="@drawable/ic_verified_user"
            android:title="About Us" />
    </group>

    <item android:title="Options">
        <menu>
            <item
                android:id="@+id/nav_pas_logout"
                android:icon="@drawable/ic_logout"
                android:title="Logout" />
            <item
                android:id="@+id/nav_pas_exit"
                android:icon="@drawable/ic_exit_to_app_black_24dp"
                android:title="Exit" />
        </menu>
    </item>

</menu>
